
```
front-end
```

---

# 🚀 PASOS EXACTOS (orden correcto)

## 1️⃣ Ir a tu carpeta de trabajo

(la que ya usas: `worship-v3`)

```bash
cd /d/Versiones-worship/worship-v3
```

---

# 2️⃣ Crear proyecto Astro llamado front-end

```bash
npm create astro@latest front-end
```

Selecciona:

```
template: minimal
typescript: yes
install deps: yes
git: yes (opcional)
```

---

# 3️⃣ Entrar al proyecto

```bash
cd front-end
```

---

# 4️⃣ Instalar Tailwind

```bash
npx astro add tailwind
```

Aceptar cambios → yes

---

# 5️⃣ Instalar React

```bash
npx astro add react
```

Aceptar → yes

---

# 6️⃣ Ejecutar proyecto

```bash
npm run dev
```

Abrir en navegador:

```
http://localhost:4321
```

---

# ✅ Estructura final correcta

```
worship-v3/
  superior-shell/
  front-end/
    astro.config.mjs
    package.json
    src/
    public/
```

---

# 🎯 Comandos resumidos (copiar y pegar)

```bash
cd /d/Versiones-worship/worship-v3
npm create astro@latest front-end
cd front-end
npx astro add tailwind
npx astro add react
npm run dev
```

---

tiempo ahorado solo en la primera version
1. Configuración de Arquitectura (Astro + React + Tailwind v4)
A mano: Configurar Astro, integrar React, configurar el nuevo Tailwind v4 (que tiene una sintaxis de configuración distinta), y organizar la estructura de carpetas.
Tiempo estimado: 1 hora.
2. Navbar con Glassmorphism y Lógica Móvil
A mano: Diseñar el CSS para el efecto de desenfoque, crear el componente React, manejar el estado del menú hamburguesa, y asegurar que sea responsive y con z-index correcto.
Tiempo estimado: 2 horas.
3. Efecto "Antigravity" de Letras
A mano: Esta es la parte más compleja. Implementar la lógica para separar cada carácter, generar valores aleatorios para x, y y rotate sin que causen parpadeos (usando useMemo), escribir las keyframes de CSS personalizadas y ajustar el timing exacto para que se vea fluido.
Tiempo estimado: 4 - 5 horas.
4. Integración de tsParticles Interactiva
A mano: Leer la documentación de @tsparticles/react, configurar el motor de partículas, ajustar las opciones de interactividad (efecto grab con el mouse) y asegurar que el canvas sea transparente para que no tape el gradiente del fondo.
Tiempo estimado: 2 horas.
5. Estética Visual y Debugging
A mano: Crear los gradientes radiales complejos, arreglar avisos de PostCSS (como el orden de los @import), y refinar las capas de z-index para que el texto sea legible sobre las partículas.
